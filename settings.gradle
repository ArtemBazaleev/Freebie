pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            name = "freebie-maven-repo"
            url = uri("https://maven.pkg.github.com/KirillAchramiomok/freebie-proto")

        }
    }
}
rootProject.name = "Freebie"
include ':app'
